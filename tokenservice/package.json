{
  "name": "tokenservice",
  "version": "1.0.0",
  "description": "token storage solution",
  "main": "index.ts",
  "license": "MIT",
  "scripts": {
    "start": "node index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
